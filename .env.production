# Production environment variables for GitHub Pages deployment
# This file should NOT be committed to git (add to .gitignore)

# API Endpoints - Update these when you deploy your backend
REACT_APP_API_URL=https://localhost:5000
REACT_APP_EXTRACT_CV_ENDPOINT=/api/extract-cv
REACT_APP_CONTACT_ENDPOINT=/api/contact
REACT_APP_VOLUNTEER_ENDPOINT=/api/volunteer-application

# Groq AI API Key - Should be set via GitHub Actions secrets, not in .env.production
# REACT_APP_GROQ_KEY is handled securely in build process

# Environment
NODE_ENV=production
